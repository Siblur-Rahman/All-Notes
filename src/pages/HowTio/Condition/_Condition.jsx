import React from 'react';
import DisplayElement from './DisplayElement';

const Condition = () => {
    return (
        <div>
            <DisplayElement/>
        </div>
    );
};

export default Condition;